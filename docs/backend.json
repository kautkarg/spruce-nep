
{
  "entities": {
    "Course": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Course",
      "type": "object",
      "description": "Represents a course offered by Spruce Life Skills.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Course entity."
        },
        "title": {
          "type": "string",
          "description": "The title of the course."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the course."
        },
        "category": {
          "type": "string",
          "description": "The category that the course belongs to (e.g., NEP)."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the image representing the course.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "title",
        "description",
        "category",
        "imageUrl"
      ]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "paths": {
      "/courses/{courseId}": {
          "schema": { "$ref": "#/entities/Course" },
          "description": "Stores information about each course."
      }
    },
    "reasoning": "This Firestore data structure stores public course information. User-specific data is no longer stored as authentication and membership features have been removed."
  }
}
